# testdata

This directory contains test application repositories used by external CI systems.

The `konveyor/koncur` integration tests reference these paths via git URL:
- `testdata/nerd-dinner` - NerdDinner MVC4 application

Do not remove this directory without updating `konveyor/koncur` `tests/nerd-dinner/test.yaml` first.
